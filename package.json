{
  "name": "digitaldash-toolkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-checks.mjs && node scripts/validate-refs.mjs"
  },
  "devDependencies": {
    "yaml": "^2.6.1"
  }
}
